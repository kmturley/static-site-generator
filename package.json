{
  "name": "static-site-generator",
  "version": "1.0.0",
  "description": "Example static site generator using a sitemap",
  "main": "./src/index.js",
  "scripts": {
    "generate": "node ./src/index.js",
    "start": "http-server ./src/example",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "static",
    "site",
    "generator",
    "sitemap"
  ],
  "author": "kmturley",
  "license": "ISC",
  "dependencies": {
    "cheerio": "^1.0.0-rc.3",
    "http-server": "^0.12.3",
    "sync-request": "^6.1.0"
  }
}
